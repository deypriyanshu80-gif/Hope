"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.releaseRateLimitHandler = exports.setRateLimitHandler = exports.getRateLimitHandler = void 0;
const errors_1 = require("../errors");
const queue_1 = require("../providers/queue");
async function getConfiguredRateLimit(_req, queue) {
    if (!queue.supportsGlobalRateLimit) {
        return { status: 200, body: { supported: false, rateLimit: null } };
    }
    return {
        status: 200,
        body: { supported: true, rateLimit: await queue.getConfiguredRateLimit() },
    };
}
async function setConfiguredRateLimit(req, queue) {
    if (!queue.supportsGlobalRateLimit) {
        return (0, errors_1.errorResponse)(400, 'ERRORS.RATE_LIMIT_NOT_SUPPORTED');
    }
    const { max, duration } = req.body;
    if (max === null || max === undefined) {
        await queue.removeConfiguredRateLimit();
        return { status: 200, body: {} };
    }
    await queue.setConfiguredRateLimit({ max, duration });
    return { status: 200, body: {} };
}
async function releaseActiveRateLimit(_req, queue) {
    await queue.releaseActiveRateLimit();
    return { status: 200, body: {} };
}
exports.getRateLimitHandler = (0, queue_1.queueProvider)(getConfiguredRateLimit, {
    skipReadOnlyModeCheck: true,
});
exports.setRateLimitHandler = (0, queue_1.queueProvider)(setConfiguredRateLimit);
exports.releaseRateLimitHandler = (0, queue_1.queueProvider)(releaseActiveRateLimit);
//# sourceMappingURL=rateLimit.js.map