"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.updateJobSchedulerHandler = void 0;
const errors_1 = require("../errors");
const queue_1 = require("../providers/queue");
/**
 * Rewrites the schedule of an existing scheduler. Only the schedule: the job the scheduler
 * produces keeps the name, data and options the application registered, so an edit here cannot
 * silently change what runs.
 */
async function updateJobScheduler(req, queue) {
    const { schedulerId } = req.params;
    if (!queue.supportsJobSchedulerUpdate) {
        return (0, errors_1.errorResponse)(405, 'ERRORS.JOB_SCHEDULER_EDIT_NOT_SUPPORTED');
    }
    const { pattern, every, tz, limit, endDate } = req.body;
    const hasPattern = typeof pattern === 'string' && pattern.trim().length > 0;
    const repeat = {
        ...(hasPattern ? { pattern: pattern.trim() } : { every: every }),
        ...(typeof tz === 'string' && tz.trim().length > 0 ? { tz: tz.trim() } : {}),
        ...(limit !== undefined && limit !== null ? { limit } : {}),
        ...(endDate !== undefined && endDate !== null ? { endDate } : {}),
    };
    const result = await queue.updateJobScheduler(schedulerId, repeat);
    if (result === 'not-found') {
        return (0, errors_1.errorResponse)(404, 'ERRORS.JOB_SCHEDULER_NOT_FOUND');
    }
    if (result === 'invalid-schedule') {
        return (0, errors_1.errorResponse)(400, 'ERRORS.INVALID_SCHEDULER_PATTERN');
    }
    return {
        status: 204,
        body: {},
    };
}
exports.updateJobSchedulerHandler = (0, queue_1.queueProvider)(updateJobScheduler);
//# sourceMappingURL=updateJobScheduler.js.map