"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateRequest = validateRequest;
exports.validateResponse = validateResponse;
const v = __importStar(require("valibot"));
const errors_1 = require("./errors");
const errorKeys_1 = require("./schemas/errorKeys");
const requests_1 = require("./schemas/requests");
const responses_1 = require("./schemas/responses");
const support_1 = require("./schemas/support");
const TRANSLATION_KEYS = new Set(errorKeys_1.ERROR_TRANSLATION_KEYS);
function issueMessage(issues, fallback) {
    const named = issues
        .map((issue) => (0, support_1.decodeKey)(issue.message))
        .find((decoded) => TRANSLATION_KEYS.has(decoded.key));
    return (named !== null && named !== void 0 ? named : { key: fallback });
}
function issuePath(issues) {
    var _a;
    const path = (_a = issues[0]) === null || _a === void 0 ? void 0 : _a.path;
    return path === null || path === void 0 ? void 0 : path.map((segment) => { var _a; return String((_a = segment.key) !== null && _a !== void 0 ? _a : ''); }).filter(Boolean).join('.');
}
function treatEmptyValuesAsAbsent(query) {
    const result = {};
    for (const [name, value] of Object.entries(query !== null && query !== void 0 ? query : {})) {
        if (value !== '') {
            result[name] = value;
        }
    }
    return result;
}
function rejection(issues, fallback) {
    const message = issueMessage(issues, fallback);
    const field = issuePath(issues);
    // Only the generic keys name the field: the specific ones are asserted verbatim by clients.
    return (0, errors_1.errorResponse)(400, message.key === fallback && field ? { key: message.key, options: { field } } : message, { code: 'INVALID_REQUEST' });
}
function validateRequest(route, request) {
    var _a;
    const { query, body } = route.spec;
    if (query) {
        const parsed = v.safeParse(requests_1.requestSchemas[query], treatEmptyValuesAsAbsent(request.query));
        if (!parsed.success) {
            return rejection(parsed.issues, 'ERRORS.INVALID_QUERY_PARAM');
        }
        request.query = parsed.output;
    }
    if (body) {
        const parsed = v.safeParse(requests_1.requestSchemas[body], (_a = request.body) !== null && _a !== void 0 ? _a : {});
        if (!parsed.success) {
            return rejection(parsed.issues, 'ERRORS.INVALID_REQUEST_BODY');
        }
        request.body = parsed.output;
    }
    return undefined;
}
function isErrorBody(body) {
    return !!body && typeof body === 'object' && 'error' in body;
}
function validateResponse(route, result) {
    var _a, _b;
    if (isErrorBody(result.body) || result.status === 204) {
        return result;
    }
    const parsed = v.safeParse(responses_1.responseSchemas[route.spec.response], result.body);
    if (!parsed.success) {
        return (0, errors_1.errorResponse)(500, 'ERRORS.INTERNAL_SERVER_ERROR', {
            code: 'RESPONSE_SCHEMA_MISMATCH',
            details: `${route.spec.response}: ${(_b = (_a = parsed.issues[0]) === null || _a === void 0 ? void 0 : _a.message) !== null && _b !== void 0 ? _b : 'did not match its schema'}`,
        });
    }
    return result;
}
//# sourceMappingURL=validation.js.map