import type { Job, JobNode } from 'bullmq';
import { BullBoardQueues } from '../types';
export interface FlowWindow {
    depth: number;
    maxChildren: number;
}
export declare function buildBoardQueueNameResolver(queues: BullBoardQueues): (job: Job) => string;
export declare function getFlowTree(queues: BullBoardQueues, boardQueueName: string, jobId: string, window: FlowWindow): Promise<JobNode | null>;
/**
 * Traverses the parent chain of a job across queues to find the flow root.
 * Returns the board queue name and job ID of the root, or null if
 * no flow root can be determined.
 */
export declare function findFlowRoot(queues: BullBoardQueues, job: Job): Promise<{
    queueName: string;
    jobId: string;
} | null>;
