"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildBoardQueueNameResolver = buildBoardQueueNameResolver;
exports.getFlowTree = getFlowTree;
exports.findFlowRoot = findFlowRoot;
function findBullMQAdapter(queues) {
    for (const adapter of queues.values()) {
        if (adapter.type === 'bullmq') {
            return adapter;
        }
    }
    return null;
}
function findBoardAdapter(queues, boardQueueName) {
    const adapter = queues.get(boardQueueName);
    return (adapter === null || adapter === void 0 ? void 0 : adapter.type) === 'bullmq' ? adapter : null;
}
// Keyed by the qualified `prefix:name`, which is what a job's `opts.parent.queue` carries and the
// only form that tells two board entries running one queue name under two prefixes apart.
function buildQueueLookup(queues) {
    const lookup = new Map();
    for (const adapter of queues.values()) {
        if (adapter.type === 'bullmq') {
            const bmq = adapter;
            lookup.set(bmq.getQueueQualifiedName(), bmq);
        }
    }
    return lookup;
}
// A queue is registered on the board under `prefix` + the name BullMQ knows it by, and job URLs
// and every other route are keyed by that rather than by the name a job reports.
function buildBoardQueueNameResolver(queues) {
    const lookup = buildQueueLookup(queues);
    return (job) => { var _a, _b; return (_b = (_a = lookup.get(job.queueQualifiedName)) === null || _a === void 0 ? void 0 : _a.getName()) !== null && _b !== void 0 ? _b : job.queueName; };
}
// The producer comes from the flow root's own adapter, so on a board mixing backends or
// connections the tree is read from the datastore it lives in. The first bullmq adapter is
// only a fallback for a root whose queue is not registered on the board.
async function getFlowTree(queues, boardQueueName, jobId, window) {
    var _a, _b;
    const adapter = findBoardAdapter(queues, boardQueueName);
    const producer = await ((_a = (adapter !== null && adapter !== void 0 ? adapter : findBullMQAdapter(queues))) === null || _a === void 0 ? void 0 : _a.getFlowProducer());
    if (!producer)
        return null;
    return await producer
        .getFlow({
        queueName: (_b = adapter === null || adapter === void 0 ? void 0 : adapter.getQueueName()) !== null && _b !== void 0 ? _b : boardQueueName,
        id: jobId,
        depth: window.depth,
        maxChildren: window.maxChildren,
    })
        .catch(() => null);
}
/**
 * Traverses the parent chain of a job across queues to find the flow root.
 * Returns the board queue name and job ID of the root, or null if
 * no flow root can be determined.
 */
async function findFlowRoot(queues, job) {
    var _a, _b;
    const lookup = buildQueueLookup(queues);
    let currJob = job;
    let currAdapter = lookup.get(job.queueQualifiedName);
    while (currJob) {
        const parent = (_a = currJob.opts) === null || _a === void 0 ? void 0 : _a.parent;
        if (!(parent === null || parent === void 0 ? void 0 : parent.id) || !(parent === null || parent === void 0 ? void 0 : parent.queue)) {
            if (!currJob.id) {
                return null;
            }
            return { queueName: (_b = currAdapter === null || currAdapter === void 0 ? void 0 : currAdapter.getName()) !== null && _b !== void 0 ? _b : currJob.queueName, jobId: currJob.id };
        }
        const parentAdapter = lookup.get(parent.queue);
        if (!parentAdapter) {
            return null;
        }
        const parentJob = await parentAdapter.getJob(parent.id);
        if (!parentJob) {
            return null;
        }
        currJob = parentJob;
        currAdapter = parentAdapter;
    }
    return null;
}
//# sourceMappingURL=flow.js.map