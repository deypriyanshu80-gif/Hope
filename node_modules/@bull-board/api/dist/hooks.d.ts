import type { ResponseSchemas } from './schemas/responses';
import type { AppControllerRoute, BoardHooks } from './types';
export declare function wrapHandler<TResponse extends keyof ResponseSchemas>(route: AppControllerRoute<TResponse>, { hooks, validateResponses }: {
    hooks?: BoardHooks;
    validateResponses: boolean;
}): AppControllerRoute<TResponse>['handler'];
