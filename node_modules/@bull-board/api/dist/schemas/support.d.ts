import * as v from 'valibot';
import type { ErrorTranslationKey } from './errorKeys';
export declare function key(translationKey: ErrorTranslationKey, options?: Record<string, unknown>): string;
export declare function decodeKey(message: string): {
    key: string;
    options?: Record<string, unknown>;
};
export declare function totalRecord<const K extends string, S extends v.GenericSchema>(keys: readonly K[], value: S): v.ObjectSchema<{ [P in K]: S; }, undefined>;
export type SchemaMap = Record<string, v.GenericSchema>;
